# npm publishing

The npm package is published as `@doggy8088/leak-hunter` and exposes the `leak-hunter` binary.

Publishing uses GitHub Actions OIDC Trusted Publishing. Do not add long-lived npm tokens such as `NPM_TOKEN`.
